import { TokenAdminDetails } from './token-admin-details';

describe('TokenAdminDetails', () => {
  it('should create an instance', () => {
    expect(new TokenAdminDetails()).toBeTruthy();
  });
});
